// This is a comment
extern input();
extern print(a);

def fib(n) {
    if (!n) {
        1;
    } else if (!(n-1)) {
        1;
    } else {
        fib(n-1) + fib(n-2);
    };
}

print(fib(input()));
