// This is a comment
extern input();
extern print(a);

def fib(n) {
    if (n) {
        if (n-1) {
            fib(n-1) + fib(n-2);
        } else {
            1;
        };
    } else {
        1;
    };
}

print(fib(input()));
